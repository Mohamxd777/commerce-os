# Cross-layer tests

Feature-level and end-to-end tests that span the client, API, and PostgreSQL database belong here. Unit and API tests stay close to their applications in `client/src/**/*.test.jsx` and `server/tests/`.

Task 1 does not yet contain a domain workflow requiring a cross-layer test.
